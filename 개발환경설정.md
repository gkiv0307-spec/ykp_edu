# 새 컴퓨터 개발환경 설정 가이드

옆커폰부동산에듀 사이트(`ykp_edu`)를 다른 컴퓨터에서도 이어서 작업하기 위한 설정 순서입니다.
위에서부터 순서대로 진행하면 됩니다. (PowerShell 창에서 명령어 입력)

---

## 1. Git 설치

```
winget install --id Git.Git -e --source winget --accept-package-agreements --accept-source-agreements
```

설치 후 **PowerShell 창을 새로 열어야** `git` 명령이 인식됩니다.

---

## 2. GitHub CLI 설치 및 로그인

```
winget install --id GitHub.cli -e --source winget --accept-package-agreements --accept-source-agreements
gh auth login --hostname github.com --git-protocol https --web
```

- 마지막 명령을 실행하면 **일회용 코드**와 **접속할 주소**가 나옵니다.
- 브라우저에서 그 주소로 들어가서, 나온 코드를 입력하고 GitHub 계정(`gkiv0307-spec`)으로 로그인하면 끝.
- 로그인 후 아래 명령으로 git이 이 로그인 정보를 자동으로 쓰게 설정:

```
gh auth setup-git
```

---

## 3. Git 사용자 정보 설정

```
git config --global user.name "gkiv0307"
git config --global user.email "gkiv0307@gmail.com"
```

---

## 4. 프로젝트 저장소 받기

원하는 폴더(예: `C:\Users\사용자명\`)로 이동한 뒤:

```
git clone https://github.com/gkiv0307-spec/ykp_edu.git
```

`ykp_edu` 폴더가 통째로 생기고, 지금까지 작업한 모든 파일(사이트 코드, 이미지 등)이 그대로 들어있습니다.

---

## 5. VSCode + Claude Code 설치

1. [VSCode 다운로드](https://code.visualstudio.com) 후 설치
2. VSCode 왼쪽 **확장(Extensions)** 아이콘 클릭 → "Claude Code" 검색 → 설치
3. 설치 후 안내에 따라 **Claude 계정으로 로그인**
4. VSCode에서 **파일 → 폴더 열기** → 4번에서 받은 `ykp_edu` 폴더 선택
5. Claude Code 패널을 열면 바로 대화 시작 가능 (이 프로젝트의 이전 작업 맥락은 없지만, 코드/파일은 전부 그대로 있음)

---

## 6. (선택) Node.js 설치

가격표 이미지/PDF 제작이나 사이트 미리보기 서버 실행 등에 필요합니다. 당장 필요 없으면 건너뛰어도 됩니다.

```
winget install --id OpenJS.NodeJS -e --source winget --accept-package-agreements --accept-source-agreements
```

---

## 참고 정보

| 항목 | 값 |
|---|---|
| GitHub 저장소 | https://github.com/gkiv0307-spec/ykp_edu |
| 실제 사이트 도메인 | http://xn--289av8kwmfs4dv2e.store (= 부동산경매.store) |
| 배포 방식 | GitHub Pages (main 브랜치에 push하면 자동 반영, 보통 1분 내) |
| 도메인 등록처 | 가비아 |
| 블로그 | blog.naver.com/ykphone_edu |
| 인스타그램 | instagram.com/ykphone_edu |
| 유튜브 | youtube.com/@ykphone_edu |

## 사이트 수정 후 배포하는 법

```
git add .
git commit -m "수정 내용 설명"
git push origin main
```

push 후 1분 정도 지나면 실제 사이트(`부동산경매.store`)에 자동 반영됩니다.
